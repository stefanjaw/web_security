<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="website_sale_navigation_buttons_inherit_web_security" model="ir.ui.view">
        <field name="name">Navigation buttons Form Inherit Web Security</field>
        <field name="priority">98</field>
        <field name="inherit_id" ref="website_sale.navigation_buttons"/>
        <field name="arch" type="xml">
            <xpath expr="//a[@name='website_sale_main_button']" position="replace">
                <t t-if="request.env.user.email_verified == False">
                    <a t-attf-href="/web/reset_password?{{ keep_query() }}"
                        t-attf-class="#{_cta_classes} btn btn-primary #{not website_sale_order._is_cart_ready() and 'disabled'} #{_form_send_navigation and 'order-lg-3 w-100 w-lg-auto ms-lg-auto' or 'w-100'}">Reset Password To Continue</a>
                </t>
                <t t-else="">
                    <a role="button" name="website_sale_main_button" t-attf-class="#{_cta_classes} btn btn-primary #{not website_sale_order._is_cart_ready() and 'disabled'} #{_form_send_navigation and 'order-lg-3 w-100 w-lg-auto ms-lg-auto' or 'w-100'}" t-att-href="step_specific_values['main_button_href']">
                        <t t-out="step_specific_values['main_button']"/>
                        <i class="fa fa-angle-right ms-2 fw-light"/>
                    </a>    
                </t>
            </xpath>
        </field>
    </record>

    <record id="payment_submit_button_inherit_web_security" model="ir.ui.view">
        <field name="name">Payment Submit Button Inherit Web Security</field>
        <field name="priority">98</field>
        <field name="inherit_id" ref="payment.submit_button"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='o_payment_submit_button']" position="replace">
                <t t-if="request.env.user.email_verified == False">
                    <a t-attf-href="/web/reset_password?{{ keep_query() }}" 
                        t-attf-class="#{_cta_classes} btn btn-primary #{not website_sale_order._is_cart_ready() and 'disabled'} #{_form_send_navigation and 'order-lg-3 w-100 w-lg-auto ms-lg-auto' or 'w-100'}">Reset Password To Continue</a>
                    <button name="o_payment_submit_button" type="submit" t-out="submit_button_label"
                        class="btn btn-primary w-100 w-md-auto ms-auto px-5" disabled="true" style="display: none;"/>
                </t>
                <t t-else="">
                    <button name="o_payment_submit_button" type="submit" t-out="submit_button_label"
                        class="btn btn-primary w-100 w-md-auto ms-auto px-5" disabled="true"/>
                </t>
            </xpath>
        </field>
    </record>

    
</odoo>
